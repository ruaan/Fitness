<%= form_for(@sappush) do |f| %>
  <% if @sappush.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sappush.errors.count, "error") %> prohibited this sappush from being saved:</h2>

      <ul>
      <% @sappush.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
